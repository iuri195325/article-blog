<%- include('partials/header.ejs'); %>

<body>
    <div class="container">
        <br>
        <hr>
        <h2>Artigos Gratis</h2>
        <hr>
        <% article.forEach(article => {  %>
            <div class="card">
                <div class="card-header">
                    <%= article.title %>
                </div>
                <div class="card-body">
                    <a href="/artigos/<%= category.slug %>" class="btn btn-primary">Ler artigo</a>
                </div>
            </div>
            <br>
         <% }) %>   
    </div>
</body>

<%- include ('partials/footer.ejs'); %>